# ============================================
# FX281 Podcast Backend - Python Dependencies (Cloud)
# 部署用轻量依赖：不含 torch/funasr/modelscope，用 DashScope 云端 ASR
# Python 3.11 required (see runtime.txt)
# ============================================

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# DashScope SDK（云端 ASR 调用用 httpx，dashscope SDK 备用）
dashscope

# DeepSeek / Qwen Chat (OpenAI-compatible mode)
openai

# HTTP client（DashScope OSS 上传 + 转写轮询）
httpx

# Data
pydantic==2.9.2

# Export
python-docx
pydub

# ffmpeg 兜底（容器内若无系统 ffmpeg）
imageio-ffmpeg

# ============================================
# Install
# ============================================
# pip install -r requirements-cloud.txt
